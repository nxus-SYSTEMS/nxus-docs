# W459 Discovery Boundary Terminology Synthesis

Private/no-deploy Docs receiver packet for RB-008 /
`NXUSKIT-V2-MEGA-20260605`, Docs W459, wave W459 of estimated W426-W492.

This discovery boundary terminology synthesis consolidates Docs W451 with the
latest Website, SDK, Licensing, Examples, Celerat, and Peeler private boundary
returns. It is a private terminology and gate packet only. It does not edit
routed docs, generate public docs, publish Website copy, deploy, merge to main,
or authorize public SDK, runtime, request-access, support, compliance, release,
package, or entitlement claims.

## Source Evidence

Docs evidence:

- Docs W451 checkpoint:
  `01c85ac88ebdfbf5bef1ddb99240e1ef3eb3315a`
- W451 posture:
  private no-public/no-deploy discovery-envelope boundary. It defines
  metadata-only and no-runtime wording constraints for Docs and future
  cross-surface copy, but does not authorize routed docs or public publication.

Website evidence:

- Website W458 checkpoint:
  `4bb4ca716315801ddc95ad431ea087c034f9d387`
- Website W452 checkpoint:
  `7c090451ff315bf748d984546a6b01db047fb65a`
- W458/W452 posture:
  Website-local evidence verifies W452 exists and remains no-production.
  Future request-access wording may discuss customer problems in conceptual
  terms only. W452/W458 do not authorize public Website route content,
  sitemap/metadata changes, request-access feature availability claims,
  Product Catalog/Odoo population, DataFusion runtime availability, provider
  registry claims, public deep-discovery claims, support/compliance claims, or
  production promotion.

SDK evidence:

- SDK W457 checkpoint:
  `a6afdb13ed5082a01f3013c14ea43940f2949d18`
- SDK W449 checkpoint:
  `1593ba406b3be45661ca14b49ec0a3dfd5f2e84a`
- W457/W449 posture:
  the only approved executable-adjacent mode remains
  `caller_provided_private_json`. W449 validates caller-supplied JSON shaped
  like the W441 private request/response envelope and returns private
  projection evidence only. W457 preserves the W448/W452 nuance and keeps
  stable SDK dependency, runtime execution, provider registry, public API,
  public schema, public CLI, wrapper, release/package, Product Catalog/Odoo,
  support, compliance, security, downstream mutation, and public artifact gates
  closed.

Licensing evidence:

- Licensing W450 checkpoint:
  `ed67214bb6480eb4192f9666e813580678683530`
- Licensing W456 checkpoint:
  `7aec9fb2a08956c19688fb07c1c692ef437bd5ff`
- W450/W456 posture:
  discovery entitlement domains remain private non-claim planning inputs.
  Product Catalog/Odoo is referenced only as a later-authorized potential grant
  source, not an active grant source. Request-supplied overrides remain
  forbidden, existing tokens receive no implicit discovery, async, DataFusion,
  demo, or v2.0.0 grants, and W449 supplied-JSON projection evidence cannot
  create grants or public feature claims.

Examples evidence:

- Examples W453 checkpoint:
  `f3600a5819ded68630a790a72b6b3440dbe2c1b9`
- W453 posture:
  Examples aligns with W449 by treating `caller_provided_private_json` as the
  only approved executable-adjacent mode. W453 is not a public export, public
  schema, public API, CLI command, wrapper surface, provider registry entry,
  runtime discovery path, DataFusion runtime path, async dispatch path,
  Product Catalog/Odoo mutation path, release/package/tag/deploy artifact,
  public docs or Website publication, support statement, compliance statement,
  or security statement.

Celerat evidence:

- Celerat W454 checkpoint:
  `4ef4f32c0eed5d919fae2a2edb00eb8ba9598326`
- W454 posture:
  Celerat/Plugins consumes W449 as private caller-provided JSON projection
  evidence only. It does not promote default bundled metadata, mutate public or
  provider projections, publish marketplace metadata, open provider-registry or
  runtime-provider authority, enable runtime discovery, enable DataFusion
  runtime execution, enable async dispatch, populate Product Catalog/Odoo
  values, expose public API/schema/CLI surfaces, or make support/compliance
  claims.

Peeler evidence:

- Peeler W455 checkpoint:
  `bedafd7488dc2b7d6aee00743de204cc9fcd0963`
- W455 posture:
  Peeler consumes W449 through private caller-provided JSON only. Product/app
  loop behavior, stable SDK dependency mode, runtime discovery, DataFusion
  runtime execution, async dispatch, SQL execution, public API/schema/CLI/
  wrapper authority, provider registry, runtime-provider execution, CE/OSS deep
  discovery, Product Catalog/Odoo mutation, release/package/deploy authority,
  support/compliance/security claims, downstream/public metadata mutation,
  static/public artifact publication, and runtime artifact consumption remain
  closed.

## No Public Authority Opened

Docs W459 is private/no-deploy and non-routed. It does not authorize public
Docs pages, generated docs, Website copy, request-access route text, SDK public
surfaces, public schemas, public CLIs, wrappers, provider registry entries,
runtime-provider behavior, release/package artifacts, Product Catalog/Odoo
feature grants, support/compliance/security claims, or production-ready
overclaims.

The safe output from W459 is a private terminology synthesis and validator. It
may guide a later no-deploy copy draft, but it is not a public launch artifact,
request-access approval, or Gate J entitlement decision.

## Safe-now Internal Terminology

Safe now means safe for private copy review, no-deploy planning, receiver
alignment, and later gated candidate drafting. It does not mean safe to publish
externally.

| Topic | Safe terminology class | Boundary |
| --- | --- | --- |
| Discovery envelope | Private discovery envelope evidence for request and metadata-only response planning. | Do not call it a public schema, public SDK API, CLI output contract, provider-registry payload, request-access feature, or release artifact. |
| Provider discovery | Private provider discovery vocabulary for caller-scoped, entitlement-aware metadata planning. | Do not claim customer-visible provider inventory, provider registry readiness, runtime-provider publication, or runtime discovery execution. |
| Data-engine discovery | Private data-engine discovery vocabulary for category/depth planning across provider, database, schema, table, column, and policy concepts. | Do not claim CE/OSS deep discovery, configured inventory, topology exposure, SQL execution, row access, or public database/schema/table/column enumeration. |
| DataFusion-connected metadata | DataFusion-connected metadata as metadata-only private discovery posture. | Do not imply DataFusion runtime, DataFusion gateway execution, DataFusion CE dependency, Arrow runtime, query execution, SELECT execution, or materialization. |
| Custom Provider Tables metadata | Custom Provider Tables metadata as private planning evidence. | Do not imply self-serve table discovery, SQL surfaces, table mutation, runtime table-provider execution, D10 expansion, or generated public artifacts. |
| C ABI bridge planning | C ABI bridge planning as a private bridge/readiness topic for envelope planning. | Do not present a full public C SDK, public C API, public schema, public wrapper, or runtime discovery surface. |
| Go parity deferral | Go parity deferral as private conformance-planned posture. | Do not present Go discovery as complete, runtime-backed, public API-ready, release-ready, or package-ready. |
| External PlanRef-only posture | External PlanRef-only posture for SQL/plan references that remain metadata-only. | Do not include inline SQL, inline plan bodies, inline rows, Arrow batches, live handles, materialized data, or runtime SQL/plan execution. |
| Caller-provided private JSON | `caller_provided_private_json` as the only approved executable-adjacent mode across W449/W453-W457. | Do not switch wording to stable private SDK dependency mode, runtime artifact consumption, product/app-loop integration, or public artifact publication. |
| No-public/no-runtime posture | no-public/no-runtime posture for discovery copy until later gates open. | Do not treat receiver alignment, examples fixtures, or Website request-access concepts as public availability claims. |

## Future Candidate Wording Classes

Future no-deploy candidate copy may be safe to draft only after
Manager/Operator approval. Candidate classes should remain conceptual and
customer-problem oriented:

- discovery as governed metadata planning, not runtime execution;
- caller-scoped and entitlement-aware evaluation, not customer-visible grants;
- request-access discussions, not self-serve enablement or availability;
- metadata-only provider and data-engine scope review, not topology exposure;
- DataFusion-connected metadata as a private planning term, not runtime
  DataFusion feature availability;
- Custom Provider Tables metadata as a private planning term, not public SQL or
  table-provider execution;
- external PlanRef-only posture, not inline SQL or execution;
- C ABI bridge planning and Go parity deferral as private readiness
  constraints, not shipped language-surface promises.

The safest public-facing ceiling, if a later gate opens, is restrained
request-access language such as discussing governed discovery needs. It should
avoid feature verbs like run, enable, connect, execute, discover now, generate,
publish, grant, or deploy.

## Denied Public Claims

The following classes are denied for routed public docs, generated docs,
Website copy, request-access route text, release notes, marketplace metadata,
provider registry, package metadata, public schema/API surfaces, Product
Catalog/Odoo copy, customer support materials, and compliance/security
materials until a later explicit gate opens them:

- runtime discovery execution;
- DataFusion runtime;
- provider registry/runtime-provider;
- public API/schema/CLI/wrapper;
- CE/OSS deep discovery;
- SQL execution/materialization/live handles/inline rows/Arrow batches;
- Product Catalog/Odoo grant/population;
- support/compliance/security;
- release/package/deploy;
- production-ready overclaims;
- demo/general-Pro entitlement expansion;
- async dispatch, callbacks, webhooks, managed queues, runtime provider
  execution, DataFusion gateway execution, Arrow runtime, query execution,
  SELECT execution, table mutation, or reverse table-provider execution;
- public release notes, changelog, package metadata, hosted AI indexes, SEO
  metadata, structured data, sitemap, or social metadata that imply discovery
  availability;
- feature grants from W449 projection validation, W453 examples alignment,
  W454 plugin metadata alignment, W455 Peeler consumption, W456 Licensing
  non-claim evidence, W458 Website verification, or W451 Docs boundary
  evidence.

Public copy must not transform private receiver checkpoints, packet fields,
projection refs, JSON field names, row identifiers, entitlement-domain refs, or
private denial labels into public product topology, public schema examples,
customer-facing feature names, provider-registry metadata, package metadata,
support evidence, compliance evidence, or security evidence.

## Request-access Copy Boundary

W459 does not approve request-access copy. It defines what a later request-
access candidate must avoid.

Allowed future direction, only after a later gate:

- describe a customer need to evaluate which metadata surfaces an agent may see
  before it acts;
- direct interested users to an existing request-access or contact flow;
- keep all language conceptual, non-runtime, and non-entitlement-granting.

Denied direction:

- imply immediate availability, self-service enablement, runtime discovery,
  provider inventory, DataFusion runtime, public deep discovery, Product
  Catalog/Odoo grant, support outcome, compliance outcome, or security
  guarantee;
- expose raw private feature values, private projection refs, or supplied JSON
  structures;
- use discovery wording as a reason to change pricing, checkout, Stripe,
  catalog, Odoo, request-access forms, support copy, SEO metadata, or public
  docs navigation without a separate public gate.

## Publication Gates

Before any routed Docs, Website, release-note, Celerat/Plugins, Examples, SDK,
Licensing, Peeler, Product Catalog, or request-access surface may use discovery
terminology externally, Manager/Operator should require:

1. Manager/Operator approval for the specific public surface and exact lane;
2. exact upstream checkpoints for SDK W457/W449-or-later, Website W458/W452-or-
   later, Docs W451/W459-or-later, Licensing W450/W456-or-later, Examples
   W453-or-later, Celerat W454-or-later, and Peeler W455-or-later;
3. exact customer-facing phrasing for any discovery or request-access text;
4. public-surface scans proving no private wave references, checkpoint refs,
   discovery-envelope field names, supplied JSON field names, DataFusion
   runtime claims, public API/schema/CLI/wrapper claims, provider registry or
   runtime-provider claims, CE/OSS deep-discovery claims, support/compliance/
   security guarantees, production-ready overclaims, Product Catalog/Odoo
   grant/population claims, or demo/general-Pro entitlement expansion leaks;
5. W458/W435 validators and any current Website durability validators if a
   Website or request-access surface is in scope;
6. Docs version and public-leak checks if Docs is in scope;
7. Astro check/build if Docs routes or generated output change;
8. route/auth/sitemap/robots/structured-data checks if Website is in scope;
9. Claude QA or the approved Codex QA fallback on the exact public diff;
10. explicit publication/deploy authority and rollback plan.

## Options And Recommendation

Option A: keep W459 private as the cross-receiver terminology synthesis.

- Lowest risk.
- Recommended now.
- Preserves W451-W458 as private boundary evidence only.

Option B: request a later no-deploy request-access candidate packet.

- Useful if Manager wants exact public-facing discovery problem statements
  staged for review.
- Must use approved customer-facing phrasing, public-surface scans, W458/W435
  validators, and no routed publication.

Option C: request public Docs or Website implementation.

- Not safe from W459 alone.
- Requires publication authority, exact public phrasing, route-specific scans,
  support/account/customer-communication review, and rollback plan.

Recommendation: choose Option A now. Treat W459 as private synthesis evidence
until discovery vocabulary, request-access wording, entitlement-domain
semantics, receiver projection behavior, customer-facing copy, and publication
authority are separately approved.

## Validation

Run from the repository root:

```bash
node --check specs/459-discovery-boundary-terminology-synthesis/validate-w459-discovery-boundary-synthesis.mjs
node specs/459-discovery-boundary-terminology-synthesis/validate-w459-discovery-boundary-synthesis.mjs
node specs/451-discovery-envelope-public-safe-no-deploy-boundary/validate-w451-discovery-envelope-boundary.mjs
npm run check:docs-version -- --explain
npm run check:public-leaks
npm run astro check
npm run build
node specs/459-discovery-boundary-terminology-synthesis/validate-w459-discovery-boundary-synthesis.mjs --dist
node specs/451-discovery-envelope-public-safe-no-deploy-boundary/validate-w451-discovery-envelope-boundary.mjs --dist
git diff --check
git diff --cached --check
```

The validator checks this private packet for required source evidence, safe-now
terminology, future candidate wording classes, denied public claims, publication
gates, placeholder tokens, local path evidence, private repo names, and secret-
like tokens. It also checks current public docs source and optional built output
for W459-specific public-surface leakage and denied authority wording.
