# Private Surface Expansion Fixture

Cedar is now a public runtime evaluator and provider registry entry. Prolog and
Scryer are public provider features. TypeScript/Zod has npm output, and the
Node wrapper preview is a public default API.
